name: symfony-blog
recipe: symfony
config:
  webroot: .
  php: '8.1'
services:
  phpmyadmin:
    type: phpmyadmin
    hosts:
      - database
  database:
    creds:
      user: symfony-blog
      password: symfony-blog
      database: symfony-blog